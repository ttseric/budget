interface JQuery{
    datepicker(options: any):any;
    datepicker(action:string, value:any):any;
    datepicker(option:string, prop:any, value:any):any;
    spinner():any;
    tooltip(options:any):any;
}